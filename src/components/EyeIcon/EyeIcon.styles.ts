import { StyleSheet } from 'react-native';

export const getStyles = () => StyleSheet.create({
    eyeIconWrapper: {
      padding: 5,
      justifyContent: 'center',
      alignItems: 'center',
    },
    eyeIcon: {
      width: 20,
      height: 20,
      tintColor: 'gray',
    },
});
