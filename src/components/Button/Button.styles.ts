import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonColor: {
      backgroundColor: '#008080',
      borderRadius: 5,
      padding: 12,
      width:'30%',
      display:'flex',
      alignItems:'center',
    },
    buttonText: {
      color: 'white',
      fontSize: 14,
      fontWeight:'bold',

    },
  });
